<script src="https://cdnjs.cloudflare.com/ajax/libs/apexcharts/3.19.3/apexcharts.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/apexcharts/3.19.3/apexcharts.min.css" />
<script>
    $(document).ready(function () {
        $("div[name=chart]").each(function (i) {
            var ori_id = $(this).attr("id");
            var chart_id = $(this).attr("id");
            chart_id = chart_id.replace("chart_", "");
            var arr_id = chart_id.split("_");
            $.getJSON("/Home/GetUserStockPrices", { user_id: arr_id[0], stock_num: arr_id[1] }, function (data) {
                var options = set_option_data(data.prices, data.predict_prices, data.dates);
                var chart = new ApexCharts(document.getElementById(ori_id), options);
                chart.render();
            });
           
        });
    });
    function set_option_data(prices, predict_prices, time_series) {
        var options = {
            series: [
                {
                    name: "Real",
                    data: prices,
                },
                {
                    name: "Predict",
                    data: predict_prices,
                }
            ],
            chart: {

                width: 300,
                toolbar: {
                    show: false,
                },
                type: 'line',
                zoom: {
                    enabled: false
                }
            },
            dataLabels: {
                enabled: false
            },
            stroke: {
                curve: 'straight',
                width: 2                
            },
            grid: {
                show: false,
                row: {
                    colors: ['#f3f3f3', 'transparent'], // takes an array which will be repeated on columns
                    opacity: 0.5
                },
            },
            xaxis: {
                categories: time_series,
                labels: {
                    show: true,
                    
                },
                tooltip: {
                    enabled: false
                }
            },
            yaxis: {                
                labels: {
                    show: false
                }
            },
            tooltip: {
                enabled: false
            }


        };
        return options;
    }
    function gochart(prices, time_series) {
        
        var divs = document.querySelectorAll('[name = "chart"]');
        divs.forEach(function (div) {
            
        });
    }
</script>

<dir class="row">
    <dir class="col-12">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="d-block w-100" src="/images/bear_bull.png" alt="bear_bull">
                </div>
                <div class="carousel-item">
                    <img class="d-block w-100" src="/images/candlestick.png" alt="candlestick">
                </div>
                <div class="carousel-item">
                    <img class="d-block w-100" src="http://placehold.it/1280x400" alt="placehold.it/1280x400">
                </div>
            </div>
            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
        
    </dir>
</dir>
<dir class="row">
    @foreach (var u in ViewBag.users)
    {
        
                <div class="panel-heading">@u.Name</div>
                
                <!-- Table -->
                <table class="table">
                    <tr>
                        <th>#</th>
                        <th>關注股票</th>
                        <th></th>
                    </tr>
                    @{
                        int count = 1;
                    }
                    @foreach (var stock in u.UserStock)
                    {
                        <tr>
                            <td>@count</td>
                            <td>
                                <a href="/Stock/StockDetails/@stock.StockNumNavigation.StockNum">
                                    @(stock.StockNumNavigation.StockName)(@stock.StockNumNavigation.StockNum)
                                </a>
                            </td>
                            <td>
                                <div name="chart" id="chart_@(u.Id)_@stock.StockNum">

                                </div>
                            </td>
                        </tr>
                        count++;
                    }


                </table>
            
    }

</dir>
